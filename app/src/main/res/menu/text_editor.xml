<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:vs="http://schemas.android.com/apk/res-auto" >

    <group android:id="@+id/general_items">
        <!--<item android:id="@+id/add_option"
            vs:showAsAction="always"
            votingSystem:showAsAction="always"
            android:icon="@drawable/content_new"
            android:title="@string/add_option_lbl"/>-->

        <item android:id="@+id/save_editor"
            vs:showAsAction="always"
            android:icon="@android:drawable/ic_menu_save"
            android:title="@string/save_editor_lbl"/>
    </group>


</menu>